<!--

Setup the index.handlebars to have the template that Handlebars can render onto.
Create a button in index.handlebars that will submit the user input into the database.

 -->
 <h1>Burgers!</h1>

<h2>Burgers I will eat!</h2>
<!-- change cats to burgers -->
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}  <!-- partial -->
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers I have eaten!</h2>
<!-- change cats to burgers -->
<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>
<!-- Don't need this here. Change to button and move to burger list items. -->
 {{!--  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="eaten" value="1" checked> Devoured!<br>
    <input type="radio" name="eaten" value="0"> Awake
  </div> --}}

  <button type="submit">Add Burger</button>
</form>
